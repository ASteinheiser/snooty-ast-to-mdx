import { refs } from '../references';

<Extract>
  * <Ref url="pipe.$addFields">{refs["pipe.$addFields"].title}</Ref>

  * <Ref url="pipe.$match">{refs["pipe.$match"].title}</Ref>

  * <Ref url="pipe.$project">{refs["pipe.$project"].title}</Ref>

  * <Ref url="pipe.$replaceRoot">{refs["pipe.$replaceRoot"].title}</Ref>

  * <Ref url="pipe.$replaceWith">{refs["pipe.$replaceWith"].title}</Ref>

  * <Ref url="pipe.$redact">{refs["pipe.$redact"].title}</Ref>

  * <Ref url="pipe.$set">{refs["pipe.$set"].title}</Ref>

  * <Ref url="pipe.$unset">{refs["pipe.$unset"].title}</Ref>
</Extract>
