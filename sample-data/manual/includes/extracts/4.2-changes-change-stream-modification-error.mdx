import { refs } from '../../references';

<Extract>
  Starting in MongoDB 4.2, change streams will throw an exception if
  the <Ref url="change-stream-modify-output">{refs["change-stream-modify-output"].title}</Ref> modifies an event's <Ref url="change-stream-event-id">{refs["change-stream-event-id"].title}</Ref> field.
</Extract>
