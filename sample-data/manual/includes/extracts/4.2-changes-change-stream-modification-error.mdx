<Extract>
  Starting in MongoDB 4.2, change streams will throw an exception if
  the <Ref url="change-stream-modify-output">change stream aggregation pipeline</Ref> modifies an event's <Ref url="change-stream-event-id">\_id</Ref> field.
</Extract>
