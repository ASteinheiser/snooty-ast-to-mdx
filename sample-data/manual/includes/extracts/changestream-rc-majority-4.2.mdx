<Extract>
  <Ref url="changeStreams">Change streams</Ref> are
  available regardless of the <Ref url="readconcern.&#x22;majority&#x22;">`"majority"`</Ref> read concern
  support; that is, read concern `majority` support can be either
  enabled (default) or <Ref url="disable-read-concern-majority">disabled</Ref>
  to use change streams.
</Extract>
