import { refs } from '../references';

Change streams are included in <Ref url="stable-api">{refs["stable-api"].title}</Ref> V1.
However, the <Ref url="change-streams-expanded-events">{refs["change-streams-expanded-events"].title}</Ref>
option is not included in Stable API V1.
