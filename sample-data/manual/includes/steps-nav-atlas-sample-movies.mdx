* For the cluster that contains the sample data,
  click <Guilabel>Browse Collections</Guilabel>.

* In the left navigation pane, select the
  `sample_mflix` database.

* Select the `movies` collection.
