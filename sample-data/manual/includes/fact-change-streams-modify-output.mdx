import ChangestreamAvailablePipelineStages from './extracts/changestream-available-pipeline-stages.mdx';
import { refs } from '../references';

You can control <Ref url="change-stream-output">{refs["change-stream-output"].title}</Ref> by
providing an array of one or more of the following pipeline stages when
configuring the change stream:

<ChangestreamAvailablePipelineStages />
