Starting in MongoDB 5.1, <SubstitutionReference name="change-streams">change streams</SubstitutionReference> are optimized, providing more
efficient resource utilization and faster execution of some aggregation
pipeline stages.
