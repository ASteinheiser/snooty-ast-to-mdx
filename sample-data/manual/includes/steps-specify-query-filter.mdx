import { refs } from '../references';

Specify the <Ref url="document-query-filter">{refs["document-query-filter"].title}</Ref>
in the <Guilabel>Filter</Guilabel> field. A query filter document uses
<Ref url="csfle-supported-query-operators">{refs["csfle-supported-query-operators"].title}</Ref> to specify
search conditions.

Copy the following query filter document into the
<Guilabel>Filter</Guilabel> search bar:
